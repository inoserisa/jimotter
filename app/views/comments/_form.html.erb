<div class="row">
  <h5>コメント</h5>
  <%= form_with model: [@post, @comment], local: true do |f| %>
    <%= f.text_area :comment_content, class: 'form-control mb-3',row: 10 %>
    <%= f.submit "送信", class: 'btn btn-primary mb-3' %>
  <% end %>
</div>